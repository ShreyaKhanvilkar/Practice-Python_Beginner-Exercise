names = [] #list to be filled
with open("test_1.txt","r") as open_file:
    line = open_file.readline().replace("\n","") #get to first line and removed \n
    while line:
        names.append(line) #add name to list
        line = open_file.readline().replace("\n","") #get to next line and remove \n
print(names)
